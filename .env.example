# Jira Credentials
JIRA_USER=your_jira_username
JIRA_PASS=your_jira_password

# Optional overrides
HEADLESS=true
TIMEOUT_MS=45000
FAST_MODE=true

# Allowed reporters (comma-separated, optional override)
# ALLOWED_REPORTERS_CSV=Name1, Name2, Name3
